<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/secondary_green"
    tools:context=".DetalleEspecieActivity">

    <TextView
        android:id="@+id/speciesDetailTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="22sp"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"
        tools:text="🐋 Ballena Jorobada (Yubarta)"/>

    <ImageView
        android:id="@+id/speciesDetailImage"
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:scaleType="centerCrop"
        android:layout_marginBottom="16dp"
        tools:src="@drawable/ballena"/>

    <TextView
        android:id="@+id/speciesDetailDescription"
        android:layout_width="match_parent"
        android:layout_height="305dp"
        android:layout_marginBottom="24dp"
        android:textSize="16sp"
        tools:text="Migración: Viajan desde la Antártida a las aguas cálidas de Gorgona para reproducirse y dar a luz a sus crías.\n\nTemporada: El mejor avistamiento es entre julio y octubre.\n\nDatos: Miden hasta 16 metros y pesan hasta 40 toneladas. Son famosas por sus saltos y cantos bajo el agua.." />

    <Button
        android:id="@+id/buttonBackDetail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:backgroundTint="@color/secondary_green"
        android:text="@string/button_back" />

</LinearLayout>